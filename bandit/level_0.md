This challenge requires us to connect to ssh using the default creds

- `ssh --help` is enough to solve this

**Solution**

```
ssh bandit.labs.overthewire.org -p2220 -l "bandit0"
```

use password _bandit0_
